# bubble-app-contracts

Smart contracts for the Bubble private-token wallet.

Contract development, compilation, tests, and deployments live in **[`wallet/`](wallet/)** (npm + `package-lock.json`). See [`wallet/README.md`](wallet/README.md) for setup, scripts, and **dependency pinning** policy (exact-pinned OpenZeppelin, `solc`, Hardhat, and upgrade tooling).
