# Refuse to install on a Node version below the engines floor in package.json,
# rather than printing an EBADENGINE warning and continuing. Safe here: 20.19 is
# the highest floor anywhere in the dependency tree, so nothing transitive can
# fail a strict check that package.json would not already fail.
engine-strict=true
